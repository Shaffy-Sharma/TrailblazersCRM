# SuitCRMApp

Playwright project
│
├── setup
│   └── Prepare authentication
│
├── login-tests
│   └── Run tests tagged @auth
│
└── chromium
    ├── Run setup first
    ├── Load saved login session
    └── Run tests except @auth